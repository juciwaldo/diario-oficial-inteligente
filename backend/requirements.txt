# ─────────────────────────────────────────────────────────────
# requirements.txt — Diário Inteligente (Backend)
# ─────────────────────────────────────────────────────────────

# API
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# HTTP / Scraping HTML
httpx==0.27.0
beautifulsoup4==4.12.3
lxml==5.2.2

# PDF (fallback)
pymupdf==1.24.5          # extração nativa de PDF (fitz)

# OCR (fallback do fallback — apenas PDFs escaneados)
pytesseract==0.3.10
Pillow==10.3.0

# Banco de dados
asyncpg==0.29.0
sqlalchemy[asyncio]==2.0.30
alembic==1.13.1

# Autenticação
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Agendador
apscheduler==3.10.4

# Notificações
python-telegram-bot==21.3

# IA — resumo dos trechos
openai==1.30.1

# Utilitários
python-dotenv==1.0.1
pydantic==2.7.1
pydantic-settings==2.2.1

# ─────────────────────────────────────────────────────────────
# INSTALAÇÃO:
# pip install -r requirements.txt
#
# TESSERACT (OCR) — instalar separado:
# Windows: https://github.com/UB-Mannheim/tesseract/wiki
#   → Baixar instalador + marcar idioma Português durante instalação
#
# Linux/Mac:
#   sudo apt install tesseract-ocr tesseract-ocr-por   (Ubuntu)
#   brew install tesseract                              (Mac)
# ─────────────────────────────────────────────────────────────
